/**
 * AI 기능 공개 flag (기능별).
 *
 * 2026-07-08 — AI 재공개 (자소서 + 활동일지 노트 요약 + 코인 UI). 유저 트리거
 * 회사 조사는 코드에서 완전 제거됨 (pre-seed 캐시 조회만 유지).
 *
 * 면접 AI (사이드바 메뉴 · BoardDetail 탭 · /interviews 라우트) 는 비공개 유지 —
 * 공개 시점에 `useInterviewAiEnabled` 를 true 로 변경 (1줄).
 *
 * 이력 — company/07_ops/ai-features-disabled.md (2026-06-24 전면 차단 → 부분 재공개).
 */
export function useAiEnabled(): boolean {
  return true
}

/**
 * 면접 AI 진입점 전용 flag — **2026-08-07 공개**.
 *
 * 품질 검증 3회차(B 세트 7케이스, 신규 시험 자료)를 통과해 켠다. 설계한 미끼 케이스로
 * 각각 확인했다 — 답변 본문의 메타 발화 0/35 · 활동 기록 오귀속 0 (주제가 완전히 다른
 * 로그 4개로 시험) · 자료에 없는 기술 단정 0 (공고엔 있고 자료엔 없는 조건) ·
 * 자료 부족 시 `material_gap` 배지 (실패 서사 없는 케이스 3건 / 있는 케이스 0건).
 *
 * 🔴 **남은 것을 알고 켠다** — 꼬리질문이 드물게 앞 질문을 재진술하고(재시도 1회로
 * 완전히는 못 막는다), 공고 요건 반영이 회차마다 흔들린다. 둘 다 "틀린 내용을 외운다"
 * 부류는 아니지만 사용자가 낸 코인만큼 못 받는 경우다.
 *
 * 🔴 **기계 채점으로 안 잡히는 축이 있다** (어투·엉뚱함·자소서 이탈). 초기 사용자의
 * 실제 세션을 열어보는 관찰이 붙어야 한다.
 */
export function useInterviewAiEnabled(): boolean {
  return true
}
